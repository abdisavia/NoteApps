// import { z } from "zod";

// export const loginSchema = z.object({
//     email:z.string().trim().min(1,{message:"email required!"}).email({message:"Invalid Email!"}),
//     password:z.string().trim().min(1,{message: "password required!"}).min(8,{message:"password must be 8 characters!"})
// })